import { propTypes } from "react-bootstrap/esm/Image";

// const SearchBar = (props) => {
    // On search button click => search for last name in employees and display search results
    // after the search results are made

    // On click search through props.employees to create searchResults 
    // Then call ...
//     props.onEmployeeSearch(searchResults);
//     return (
//         <nav className="navbar navbar-light bg-light">
//             <form className="form-inline">
//                     <button className="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
//         </form>
//         </nav> 
// )

// }

// export default SearchBar;
